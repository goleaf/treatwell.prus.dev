version: 1

rules:
  - name: Generic API Key
    id: generic-api-key
    pattern: '(?i)(api|token|secret|key)[^\r\n]{0,20}([A-Za-z0-9_\-]{32,})'
    secret-group: 2
    severity: high
    tags:
      - key
      - token
    push-protection: enabled
    allowlist:
      regexes:
        - 'example'
