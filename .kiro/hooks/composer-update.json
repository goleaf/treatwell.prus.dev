{
  "name": "Composer Update Reminder",
  "description": "Reminds to run composer install when composer.json is modified",
  "trigger": {
    "type": "file_modified",
    "pattern": "composer.json"
  },
  "action": {
    "type": "message",
    "content": "composer.json has been modified. Run `composer install` to update dependencies. If you added new packages, consider running `composer dump-autoload` as well."
  },
  "enabled": true
}